kind: app
version: v1
spec:
  name: device
  displayname: UC-81xx device
  description: MOXA uc81xx series device
  version: "${VERSION}"
  arch: ${ARCH}
  category: device
  attributes:
    - started
    - installed

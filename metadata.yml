kind: app
version: v1
spec:
  name: device
  displayname: Device
  description: ThingsPro device app.
  version: "${VERSION}"
  arch: ${ARCH}
  category: device
  attributes:
    - started
    - installed
